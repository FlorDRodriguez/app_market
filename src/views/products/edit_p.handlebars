<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/products/edit/{{ product.id }}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{ product.nombre }} " class="form-control" name="nombre" placeholder="Producto" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{ product.brand }} " class="form-control" name="brand" placeholder="Marca">
                        </div>
                        <div class="form-group">
                            <input type="text" value="{{ product.min_desc }} " class="form-control" name="min_desc" placeholder="Descripción">
                        </div>
                        <div class="form-group">
                            <input type="number" value="{{ product.quantity }}" id="quantity" name="Cantidad" min="1" max="100">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>