{{#if success}}
  <div class="container">
    <div class="row">
      <div class="col-md-4 mx-auto">
        <div class="alert alert-success alert-dismmisible fade show" role="alert" >
          {{!dismmisible para que aparezca el icono y podamos cerrarlo
          fade para que el mje se desvanesca
          show para que se muestre}}
          {{success}}
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">{{! botoón x para cerrar}}
            <span aria-hide="true">&times;</span>
          </button>
        </div>
      </div>
    </div>
  </div>
  {{/if}}

  {{#if message}}
  <div class="container">
    <div class="row">
      <div class="col-md-4 mx-auto">
        <div class="alert alert-danger alert-dismmisible fade show" role="alert" >
          {{!dismmisible para que aparezca el icono y podamos cerrarlo
          fade para que el mje se desvanesca
          show para que se muestre}}
          {{message}}
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">{{! botoón x para cerrar}}
            <span aria-hide="true">&times;</span>
          </button>
        </div>
      </div>
    </div>
  </div>
  {{/if}}

  